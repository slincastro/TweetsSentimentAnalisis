# TweetsSentimentAnalisis

* Create Virtual environment :
   ` python3 -m venv .`
* Run Virtual environment :
    `source bin/activate`

* Install Pandas : 
    `pip install pandas` 
    `pip install matplotlib`